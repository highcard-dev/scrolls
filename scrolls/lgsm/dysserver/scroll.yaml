name: registry-1.docker.io/highcard/scroll-lgsm
desc: dysserver
version: 0.0.2
app_version: dysserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./dysserver
              - start
          - mode: exec-tty
            data:
              - ./dysserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./dysserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - dysserver
          - mode: exec
            data:
              - ./dysserver
              - auto-install
