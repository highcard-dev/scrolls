name: registry-1.docker.io/highcard/scroll-lgsm
desc: mcserver
version: 0.0.2
app_version: mcserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./mcserver
              - start
          - mode: exec-tty
            data:
              - ./mcserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./mcserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - mcserver
          - mode: exec
            data:
              - ./mcserver
              - auto-install