name: registry-1.docker.io/highcard/scroll-lgsm
desc: gmodserver
version: 0.0.2
app_version: gmodserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./gmodserver
              - update
          - mode: exec
            data:
              - ./gmodserver
              - start
          - mode: exec-tty
            data:
              - ./gmodserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./gmodserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - gmodserver
          - mode: exec
            data:
              - ./gmodserver
              - auto-install