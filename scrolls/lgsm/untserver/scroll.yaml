name: registry-1.docker.io/highcard/scroll-lgsm
desc: untserver
version: 0.0.2
app_version: untserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./untserver
              - update
          - mode: exec
            data:
              - ./untserver
              - start
          - mode: exec-tty
            data:
              - ./untserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./untserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - untserver
          - mode: exec
            data:
              - ./untserver
              - auto-install
