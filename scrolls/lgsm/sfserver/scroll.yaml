name: registry-1.docker.io/highcard/scroll-lgsm
desc: sfserver
version: 0.0.2
app_version: sfserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./sfserver
              - update
          - mode: exec
            data:
              - ./sfserver
              - start
          - mode: exec-tty
            data:
              - ./sfserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./sfserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - sfserver
          - mode: exec
            data:
              - ./sfserver
              - auto-install