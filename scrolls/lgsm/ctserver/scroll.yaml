name: registry-1.docker.io/highcard/scroll-lgsm
desc: ctserver
version: 0.0.2
app_version: ctserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./ctserver
              - update
          - mode: exec
            data:
              - ./ctserver
              - start
          - mode: exec-tty
            data:
              - ./ctserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./ctserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - ctserver
          - mode: exec
            data:
              - ./ctserver
              - auto-install