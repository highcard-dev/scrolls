name: registry-1.docker.io/highcard/scroll-lgsm
desc: sdtdserver
version: 0.0.2
app_version: sdtdserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./sdtdserver
              - update
          - mode: exec
            data:
              - ./sdtdserver
              - start
          - mode: exec-tty
            data:
              - ./sdtdserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./sdtdserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - sdtdserver
          - mode: exec
            data:
              - ./sdtdserver
              - auto-install