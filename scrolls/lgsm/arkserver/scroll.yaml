name: registry-1.docker.io/highcard/scroll-lgsm
desc: arkserver
version: 0.0.2
app_version: arkserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./arkserver
              - update
          - mode: exec
            data:
              - ./arkserver
              - start
          - mode: exec-tty
            data:
              - ./arkserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./arkserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - arkserver
          - mode: exec
            data:
              - ./arkserver
              - auto-install
