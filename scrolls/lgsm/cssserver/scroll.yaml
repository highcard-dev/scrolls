name: registry-1.docker.io/highcard/scroll-lgsm
desc: cssserver
version: 0.0.2
app_version: cssserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./cssserver
              - start
          - mode: exec-tty
            data:
              - ./cssserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./cssserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - cssserver
          - mode: exec
            data:
              - ./cssserver
              - auto-install
