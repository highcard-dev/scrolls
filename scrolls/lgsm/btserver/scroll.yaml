name: registry-1.docker.io/highcard/scroll-lgsm
desc: btserver
version: 0.0.2
app_version: btserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./btserver
              - update
          - mode: exec
            data:
              - ./btserver
              - start
          - mode: exec-tty
            data:
              - ./btserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./btserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - btserver
          - mode: exec
            data:
              - ./btserver
              - auto-install
