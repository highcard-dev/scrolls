name: registry-1.docker.io/highcard/scroll-lgsm
desc: tiserver
version: 0.0.2
app_version: tiserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./tiserver
              - start
          - mode: exec-tty
            data:
              - ./tiserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./tiserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - tiserver
          - mode: exec
            data:
              - ./tiserver
              - auto-install