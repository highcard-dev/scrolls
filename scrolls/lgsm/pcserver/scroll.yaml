name: registry-1.docker.io/highcard/scroll-lgsm
desc: pcserver
version: 0.0.2
app_version: pcserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./pcserver
              - start
          - mode: exec-tty
            data:
              - ./pcserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./pcserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - pcserver
          - mode: exec
            data:
              - ./pcserver
              - auto-install
