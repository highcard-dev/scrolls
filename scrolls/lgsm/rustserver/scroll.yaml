name: registry-1.docker.io/highcard/scroll-lgsm
desc: rustserver
version: 0.0.2
app_version: rustserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./rustserver
              - start
          - mode: exec-tty
            data:
              - ./rustserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./rustserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - rustserver
          - mode: exec
            data:
              - ./rustserver
              - auto-install
