name: registry-1.docker.io/highcard/scroll-lgsm
desc: sof2server
version: 0.0.2
app_version: sof2server
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./sof2server
              - start
          - mode: exec-tty
            data:
              - ./sof2server
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./sof2server
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - sof2server
          - mode: exec
            data:
              - ./sof2server
              - auto-install