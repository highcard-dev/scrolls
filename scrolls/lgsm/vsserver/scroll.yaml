name: registry-1.docker.io/highcard/scroll-lgsm
desc: vsserver
version: 0.0.2
app_version: vsserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./vsserver
              - start
          - mode: exec-tty
            data:
              - ./vsserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./vsserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - vsserver
          - mode: exec
            data:
              - ./vsserver
              - auto-install
