name: registry-1.docker.io/highcard/scroll-lgsm
desc: mtaserver
version: 0.0.2
app_version: mtaserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./mtaserver
              - update
          - mode: exec
            data:
              - ./mtaserver
              - start
          - mode: exec-tty
            data:
              - ./mtaserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./mtaserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - mtaserver
          - mode: exec
            data:
              - ./mtaserver
              - auto-install