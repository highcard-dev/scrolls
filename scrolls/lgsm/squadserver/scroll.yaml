name: registry-1.docker.io/highcard/scroll-lgsm
desc: squadserver
version: 0.0.2
app_version: squadserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./squadserver
              - update
          - mode: exec
            data:
              - ./squadserver
              - start
          - mode: exec-tty
            data:
              - ./squadserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./squadserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - squadserver
          - mode: exec
            data:
              - ./squadserver
              - auto-install
