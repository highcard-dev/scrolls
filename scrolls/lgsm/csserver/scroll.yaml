name: registry-1.docker.io/highcard/scroll-lgsm
desc: csserver
version: 0.0.2
app_version: csserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./csserver
              - start
          - mode: exec-tty
            data:
              - ./csserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./csserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - csserver
          - mode: exec
            data:
              - ./csserver
              - auto-install
