name: registry-1.docker.io/highcard/scroll-lgsm
desc: tfserver
version: 0.0.2
app_version: tfserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./tfserver
              - update
          - mode: exec
            data:
              - ./tfserver
              - start
          - mode: exec-tty
            data:
              - ./tfserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./tfserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - tfserver
          - mode: exec
            data:
              - ./tfserver
              - auto-install
