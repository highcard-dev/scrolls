name: registry-1.docker.io/highcard/scroll-lgsm
desc: insserver
version: 0.0.2
app_version: insserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./insserver
              - start
          - mode: exec-tty
            data:
              - ./insserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./insserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - insserver
          - mode: exec
            data:
              - ./insserver
              - auto-install
