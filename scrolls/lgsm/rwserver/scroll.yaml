name: registry-1.docker.io/highcard/scroll-lgsm
desc: rwserver
version: 0.0.2
app_version: rwserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./rwserver
              - start
          - mode: exec-tty
            data:
              - ./rwserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./rwserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - rwserver
          - mode: exec
            data:
              - ./rwserver
              - auto-install
