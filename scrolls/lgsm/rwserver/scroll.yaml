name: registry-1.docker.io/highcard/scroll-lgsm
desc: rwserver
version: 0.0.2
app_version: rwserver
init: "main.install"
processes:
  main:
    commands:
      start:
        should_change_status: start
        procedures:
          - mode: exec
            data:
              - ./rwserver
              - start
          - mode: exec-tty
            data:
              - ./rwserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./rwserver
              - stop
      install:
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - rwserver
          - mode: exec
            data:
              - ./rwserver
              - auto-install
          - mode: command
            data: start
