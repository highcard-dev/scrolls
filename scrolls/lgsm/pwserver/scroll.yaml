name: registry-1.docker.io/highcard/scroll-lgsm
desc: pwserver
version: 0.0.2
app_version: pwserver
init: "main.install"
processes:
  main:
    commands:
      start:
        should_change_status: start
        procedures:
          - mode: exec
            data:
              - ./pwserver
              - start
          - mode: exec-tty
            data:
              - ./pwserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./pwserver
              - stop
      install:
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - pwserver
          - mode: exec
            data:
              - ./pwserver
              - auto-install
          - mode: command
            data: start
