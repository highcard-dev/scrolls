name: registry-1.docker.io/highcard/scroll-lgsm
desc: dodsserver
version: 0.0.2
app_version: dodsserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./dodsserver
              - start
          - mode: exec-tty
            data:
              - ./dodsserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./dodsserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - dodsserver
          - mode: exec
            data:
              - ./dodsserver
              - auto-install
