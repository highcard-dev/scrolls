name: registry-1.docker.io/highcard/scroll-lgsm
desc: dodsserver
version: 0.0.2
app_version: dodsserver
init: "main.install"
processes:
  main:
    commands:
      start:
        should_change_status: start
        procedures:
          - mode: exec
            data:
              - ./dodsserver
              - start
      console:
        procedures:
          - mode: exec
            data:
              - sh
              - attach.sh
      stop:
        procedures:
          - mode: exec
            data:
              - ./dodsserver
              - stop
      install:
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - dodsserver
          - mode: exec
            data:
              - ./dodsserver
              - auto-install
          - mode: command
            data: start
