name: registry-1.docker.io/highcard/scroll-lgsm
desc: sbserver
version: 0.0.2
app_version: sbserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./sbserver
              - start
          - mode: exec-tty
            data:
              - ./sbserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./sbserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - sbserver
          - mode: exec
            data:
              - ./sbserver
              - auto-install
