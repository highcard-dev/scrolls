name: registry-1.docker.io/highcard/scroll-lgsm
desc: dmcserver
version: 0.0.2
app_version: dmcserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./dmcserver
              - start
          - mode: exec-tty
            data:
              - ./dmcserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./dmcserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - dmcserver
          - mode: exec
            data:
              - ./dmcserver
              - auto-install
