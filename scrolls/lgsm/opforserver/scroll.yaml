name: registry-1.docker.io/highcard/scroll-lgsm
desc: opforserver
version: 0.0.2
app_version: opforserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./opforserver
              - update
          - mode: exec
            data:
              - ./opforserver
              - start
          - mode: exec-tty
            data:
              - ./opforserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./opforserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - opforserver
          - mode: exec
            data:
              - ./opforserver
              - auto-install