name: registry-1.docker.io/highcard/scroll-lgsm
desc: hldmsserver
version: 0.0.2
app_version: hldmsserver
init: "main.install"
processes:
  main:
    commands:
      start:
        should_change_status: start
        procedures:
          - mode: exec
            data:
              - ./hldmsserver
              - start
          - mode: exec-tty
            data:
              - ./hldmsserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./hldmsserver
              - stop
      install:
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - hldmsserver
          - mode: exec
            data:
              - ./hldmsserver
              - auto-install
          - mode: command
            data: start
