name: registry-1.docker.io/highcard/scroll-lgsm
desc: hldmsserver
version: 0.0.2
app_version: hldmsserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./hldmsserver
              - start
          - mode: exec-tty
            data:
              - ./hldmsserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./hldmsserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - hldmsserver
          - mode: exec
            data:
              - ./hldmsserver
              - auto-install
