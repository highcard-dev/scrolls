name: registry-1.docker.io/highcard/scroll-lgsm
desc: cmwserver
version: 0.0.2
app_version: cmwserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./cmwserver
              - start
          - mode: exec-tty
            data:
              - ./cmwserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./cmwserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - cmwserver
          - mode: exec
            data:
              - ./cmwserver
              - auto-install
