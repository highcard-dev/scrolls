name: registry-1.docker.io/highcard/scroll-lgsm
desc: sampserver
version: 0.0.2
app_version: sampserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./sampserver
              - start
          - mode: exec-tty
            data:
              - ./sampserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./sampserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - sampserver
          - mode: exec
            data:
              - ./sampserver
              - auto-install
