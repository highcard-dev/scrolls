name: registry-1.docker.io/highcard/scroll-lgsm
desc: tfcserver
version: 0.0.2
app_version: tfcserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./tfcserver
              - update
          - mode: exec
            data:
              - ./tfcserver
              - start
          - mode: exec-tty
            data:
              - ./tfcserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./tfcserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - tfcserver
          - mode: exec
            data:
              - ./tfcserver
              - auto-install