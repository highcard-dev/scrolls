name: registry-1.docker.io/highcard/scroll-lgsm
desc: scpslserver
version: 0.0.2
app_version: scpslserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./scpslserver
              - update
          - mode: exec
            data:
              - ./scpslserver
              - start
          - mode: exec-tty
            data:
              - ./scpslserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./scpslserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - scpslserver
          - mode: exec
            data:
              - ./scpslserver
              - auto-install