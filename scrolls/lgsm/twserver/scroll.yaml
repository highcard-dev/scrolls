name: registry-1.docker.io/highcard/scroll-lgsm
desc: twserver
version: 0.0.2
app_version: twserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./twserver
              - start
          - mode: exec-tty
            data:
              - ./twserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./twserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - twserver
          - mode: exec
            data:
              - ./twserver
              - auto-install
