name: registry-1.docker.io/highcard/scroll-lgsm
desc: dodserver
version: 0.0.2
app_version: dodserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./dodserver
              - start
          - mode: exec-tty
            data:
              - ./dodserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./dodserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - dodserver
          - mode: exec
            data:
              - ./dodserver
              - auto-install