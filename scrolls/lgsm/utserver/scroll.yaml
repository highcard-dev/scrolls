name: registry-1.docker.io/highcard/scroll-lgsm
desc: utserver
version: 0.0.2
app_version: utserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./utserver
              - start
          - mode: exec-tty
            data:
              - ./utserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./utserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - utserver
          - mode: exec
            data:
              - ./utserver
              - auto-install
