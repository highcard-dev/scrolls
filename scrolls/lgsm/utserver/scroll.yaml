name: registry-1.docker.io/highcard/scroll-lgsm
desc: utserver
version: 0.0.2
app_version: utserver
init: "main.install"
processes:
  main:
    commands:
      start:
        should_change_status: start
        procedures:
          - mode: exec
            data:
              - ./utserver
              - start
          - mode: exec-tty
            data:
              - ./utserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./utserver
              - stop
      install:
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - utserver
          - mode: exec
            data:
              - ./utserver
              - auto-install
          - mode: command
            data: start
