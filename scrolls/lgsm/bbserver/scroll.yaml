name: registry-1.docker.io/highcard/scroll-lgsm
desc: bbserver
version: 0.0.2
app_version: bbserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./bbserver
              - start
          - mode: exec-tty
            data:
              - ./bbserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./bbserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - bbserver
          - mode: exec
            data:
              - ./bbserver
              - auto-install
