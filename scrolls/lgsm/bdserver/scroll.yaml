name: registry-1.docker.io/highcard/scroll-lgsm
desc: bdserver
version: 0.0.2
app_version: bdserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./bdserver
              - update
          - mode: exec
            data:
              - ./bdserver
              - start
          - mode: exec-tty
            data:
              - ./bdserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./bdserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - bdserver
          - mode: exec
            data:
              - ./bdserver
              - auto-install
