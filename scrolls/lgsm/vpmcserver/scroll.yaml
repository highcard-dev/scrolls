name: registry-1.docker.io/highcard/scroll-lgsm
desc: vpmcserver
version: 0.0.2
app_version: vpmcserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./vpmcserver
              - start
          - mode: exec-tty
            data:
              - ./vpmcserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./vpmcserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - vpmcserver
          - mode: exec
            data:
              - ./vpmcserver
              - auto-install
