name: registry-1.docker.io/highcard/scroll-lgsm
desc: zpsserver
version: 0.0.2
app_version: zpsserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./zpsserver
              - start
          - mode: exec-tty
            data:
              - ./zpsserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./zpsserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - zpsserver
          - mode: exec
            data:
              - ./zpsserver
              - auto-install
