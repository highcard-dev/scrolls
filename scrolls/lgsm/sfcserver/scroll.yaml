name: registry-1.docker.io/highcard/scroll-lgsm
desc: sfcserver
version: 0.0.2
app_version: sfcserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./sfcserver
              - start
          - mode: exec-tty
            data:
              - ./sfcserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./sfcserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - sfcserver
          - mode: exec
            data:
              - ./sfcserver
              - auto-install
