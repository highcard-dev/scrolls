name: registry-1.docker.io/highcard/scroll-lgsm
desc: tuserver
version: 0.0.2
app_version: tuserver
init: "main.start"
processes:
  main:
    commands:
      start:
        need: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./tuserver
              - start
          - mode: exec-tty
            data:
              - ./tuserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./tuserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - tuserver
          - mode: exec
            data:
              - ./tuserver
              - auto-install
