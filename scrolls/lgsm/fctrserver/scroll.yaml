name: registry-1.docker.io/highcard/scroll-lgsm
desc: fctrserver
version: 0.0.2
app_version: fctrserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./fctrserver
              - update
          - mode: exec
            data:
              - ./fctrserver
              - start
          - mode: exec-tty
            data:
              - ./fctrserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./fctrserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - fctrserver
          - mode: exec
            data:
              - ./fctrserver
              - auto-install