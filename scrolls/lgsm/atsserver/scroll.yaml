name: registry-1.docker.io/highcard/scroll-lgsm
desc: atsserver
version: 0.0.2
app_version: atsserver
init: "main.start"
processes:
  main:
    commands:
      start:
        needs: [main.install]
        run: restart
        procedures:
          - mode: exec
            data:
              - ./atsserver
              - update
          - mode: exec
            data:
              - ./atsserver
              - start
          - mode: exec-tty
            data:
              - ./atsserver
              - console
      stop:
        procedures:
          - mode: exec
            data:
              - ./atsserver
              - stop
      install:
        run: once
        procedures:
          - mode: exec
            data:
              - sh
              - install-lgsm.sh
          - mode: exec
            data:
              - ./linuxgsm.sh
              - atsserver
          - mode: exec
            data:
              - ./atsserver
              - auto-install
